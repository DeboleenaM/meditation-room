// vite.config.js
// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  assetsInclude: ['**/*.glb', '**/*.mp3', '**/*.jpg', '**/*.png'], // allow .glb and other assets
});

